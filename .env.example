# Optional — also configurable in Settings (stored under config/ for portability)
GEMINI_API_KEY=

# Force portable root (default: process.cwd() or exe directory)
# INDEXARC_ROOT=

# PORT=3000
# HOST=127.0.0.1
